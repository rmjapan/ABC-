
#include <bits/stdc++.h>

using namespace std;
using ll=long long;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
int main() {
int N;
cin>>N;
   ///上に行くとx座標,下でy座標にいく　でこれも場合の数的に考えることができるけど全探索だと吉備ちいね。2^N
   //長さNの正整数列Aが与えられます.
   今,x=A1,y=0である.
   N回の操作を実行できる.
   しかし,条件がある。
i回目の操作はx=A[i]または,y=A[i]のどちらかしかできない.(i=1~N)

N回の動作後 X=x　Y=yになれば　Yesと表示し,そうでなければNOと表示するプログラム
   //整数X,Yが与えられ,
   //N+1個の2つの数字の組P=(P1,P2,・・・PN+1)
    

    return 0;
}